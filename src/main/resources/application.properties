spring.jpa.open-in-view=false

# Lite Mode: H2 console for local debugging (optional), otherwise handled by environment
# Database connection settings should be provided by environment variables in Production (Railway)

# Logging
logging.level.root=WARN
logging.level.com.bus_tracker=INFO
